{
  "name": "bielu-github-actions-next-version",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Computes prerelease versions from a changesets workspace's pending changesets. Self-contained on purpose: nextVersion.yml installs it here and points it at the caller's repository, so the caller needs nothing installed and is never mutated.",
  "dependencies": {
    "@changesets/get-release-plan": "^4.0.16",
    "@manypkg/get-packages": "^1.1.3",
    "semver": "^7.8.5"
  }
}
